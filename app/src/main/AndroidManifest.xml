<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leadmen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.AuditingActivity"></activity>
        <activity android:name=".Activities.AddExtraFieldsActivity" />
        <activity android:name=".Activities.ExpenseActivity" />
        <activity android:name=".Activities.EditProfileActivity" />
        <activity android:name=".Activities.AddNewBranchActivity" />
        <activity android:name=".Activities.NewBranchActivity" />
        <activity android:name=".Activities.TopupActivity" />
        <activity android:name=".Activities.NewLineActivity" />
        <activity android:name=".Activities.EmptyActivity" />
        <activity android:name=".Activities.DailyActivity" />
        <activity android:name=".Activities.NewLoanActivity" />
        <activity android:name=".Activities.CollectionsActivity" />
        <activity android:name=".Activities.ReportActivity" />
        <activity android:name=".Activities.RequsetFormActivity" />
        <activity android:name=".Activities.MyLoansActivity" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity
            android:name=".Activities.DashboardActivity"
            android:label="@string/title_activity_dashboard" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity
            android:name=".Activities.MainActivity"
            android:theme="@style/AppTheme.GreenStatusBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>